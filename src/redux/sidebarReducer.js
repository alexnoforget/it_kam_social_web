const initialState = {
  friends: [
    { id: 1, name: 'Fedor' },
    { id: 3, name: 'Petya' },
    { id: 4, name: 'Pasha' },
  ],
}

export const sidebarReducer = (state = initialState, action) => {
  return state
}